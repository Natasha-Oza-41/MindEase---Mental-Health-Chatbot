# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Static file serving (needed for frontend)
aiofiles>=23.0.0

# Data Validation
pydantic>=2.9.0
pydantic-settings>=2.5.0

# LLM Client (OpenAI-compatible — works with Ollama, Groq, Together AI, etc.)
openai>=1.51.0

# Vector Store
# Note: chromadb>=0.6.0 ships pre-built wheels for Python 3.13 on Windows.
chromadb>=0.6.0

# Embeddings (local, no API key — downloads ~80MB model on first run)
sentence-transformers>=3.0.0

# Environment Variables
python-dotenv>=1.0.0

# HTTP Client
httpx>=0.27.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
